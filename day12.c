#include<stdio.h>
#include<stdlib.h>





// POINTER 

/*
int main(void)
{
    int num=20;
    int *ptr; // pointer declaration(pointer variable)
    ptr=&num; // pointer definition

int **pptr=&ptr; // pointer to pointer 
// it is holding the address of ptr

    printf("Num:%d *ptr:%d",num,*ptr);
    printf("\n&Num:%u ptr:%u",&num,ptr);
    printf("\n&ptr=%u",&ptr);
    printf("\nSize:%d",sizeof(ptr));  // as the size of compiler is 32 bit, it will give 4 bytes always
    printf("\nNum:%d **PPTR:%d",num,**pptr);
    printf("\nPPTR:%u &ptr:%u",pptr,&ptr);
    printf("\n&PPTR:%u",&pptr);
    printf("\n**PPTR:%d num=%d *ptr=%d",**pptr,num,*ptr);
    num=30;
    num++;
    printf("\n**PPTR:%d num=%d *ptr=%d",**pptr,num,*ptr);
    return 0;
}
*/



















